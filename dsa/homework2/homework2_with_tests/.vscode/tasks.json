{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "make main",
            "type": "shell",
            "command": "make main",
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}